# 变更日志

> 本文件只记录**里程碑级**变更（新增能力与机制调整），面向使用者。**每条只写「改了什么」**——缘由、证据与实现细节见 git 提交。

## 2.2 · 2026-09-22 · 跨角色协作机制：审查队列 + 闭环门禁 + 返工台账

- **信道**：保留子 Agent **直接互发消息**（不引入 PM 中转）；跨角色消息只传「问题 + 证据位置」，不复制正文
- **审查队列串行**：`global-rule` §十一 重写为「审查队列、闭环与返工台账」——一次只放行一个审查维度（前一个闭环完成才放行下一个），顺序按「破坏力降序」重排（dev 恒 `tester → reviewer`）；仅 `手动确认` 模式在首次交付弹窗**授权并行**，须满足 4 条准入（不改被审产物 / 同 `定稿 v{N}` / 期间不得驱动返工 / 批次结束一次性返工）；纯只读维度（写作 `market` / `commercial`）不受约束
- **闭环门禁**：完成信号必须携带 `未闭环问题数`，**> 0 不算完成**；编排方放行前断言「前序报告定稿 + 未闭环数 = 0 + `depends_on` 存在且定稿」；「不适用（环境限制 / 用户跳过）」与「已报缺陷未闭环」必须区分
- **返工台账（计数落盘）**：问题由**提出方**在自身报告 `## 问题清单` 登记唯一 `issue-ID`（含轮次 / 复现版本），产出方在 `## 修复记录` 引用该 ID；**轮次由提出方递增**；`未登记 = 未发生`（写入 §三 硬约束）
- **收敛出口**：issue 轮次 ≥ 3 或修复记录 ≥ 5 → **提出方直接向用户提问**（替代原「消息携带 `返工轮次: {N}/5`、由 PM 复核」的易失口径）；用户任意时刻可下达裁定并冻结该问题往返
- **文档一致性**：`tool-report` 元信息新增 `状态: 编辑中|定稿` 与 `版本: v{N}`——只有定稿版本可作为下游输入，定稿后修改一律整份覆盖并升版本；新增「落盘一致性」「返工台账」两节
- **手动模式补漏**：`手动确认` 的**返工环不弹窗**（改由台账计数自动拦、超限才弹），修正原「弹窗只盖完成信号、返工环无人看管」的漏洞
- **边界收敛**：`designer` 去掉对下游实现 skill 的具名引用、新增「项目创建动作 / 提示打开 IDE」排除项；`role-reviewer` 的「允许合并」只留判据、实例下沉平台 skill；`pt-hm-tester` 合并收口问限定「跳过」分支；写作 `market` / `commercial` 边界句改称维度名

## 2.1 · 2026-09-20 · ai-team Skill 体系审计整改

- **分层纠偏**：`ai-team-role-maker` 移除基座内 4 处开发领域工具（`ai-team-dev-tool-*`）具名引用，改为「按领域扩展提供」占位；同步在 `ai-team-dev-role-coder` 补回 `minimal-code` / `security` 加载，dev 行为不变
- **通用层去领域化**：`ai-team-tool-report` 将「开发 / 写作领域」报告模板章节下沉为指针（各报告结构由对应领域角色 skill 定义），177 → 129 行；保留元信息模板与领域目录表
- **路由一致性**：`ai-team-dev/platform-map.md` 新增「UI 驱动（按需）」列（harmony → `ai-team-dev-pt-hm-ui-test`）；`ai-team-dev-role-tester` 由硬编码平台 skill 改为查表加载
- **单一事实源**：`ai-team-dev/README.md` 去除与 `platform-map.md` / `auto-tune` / `global-rule` 重复或矛盾的表述（选项 2-4、CHANGELOG 自动管理、平台映射表）；补全 `ui-test` / `env` 清单条目；「编码模板 6 个」改为「按需新增」
- **文档卫生**：删除 `ai-team` / `ai-team-dev` / `ai-team-write` SKILL.md 的「版本历史见根 README.md」悬空指针及「最后更新」时间戳
- **体积规范放宽**：单文件门禁由「≤200 行」调整为「≤250 行，可超出 1–50 行（上限 300 行）」，同步 `my-skill-create` 与规范记忆
- **拆分**：`ai-team-dev-pt-hm-arkts-coding-rules` 358 → 269 行（仅留类型系统 / 禁用语法 / 编码规范）；新增 `ai-team-dev-pt-hm-arkts-v2-reactive`（100 行）承载 `@ObservedV2` / `@Trace`，选 V2 模板或 V2 状态管理时按需加载
- **引用规范**：全体系清除「上游调用关系」描述（被编排方 spawn / 由 `ai-team-role-X` 加载 / 由 PM 引用 等），改为条件式触发，覆盖 3 基座 + 5 工具 + 11 领域扩展 + 3 平台角色
- **单写门禁**：`ai-team-dev-pt-hm-ui-test` 报告落盘改为按调用角色写（coder → `coder-report.md`，tester → `tester-report.md`）

## 2.0 · 2026-09-18

### 09-18 · 写作领域落地 + 角色层通配化重构

- **新增写作领域** `ai-team-write`：7 角色（主编 / 设定 / 执笔 / 审稿 / 读者团 / 竞品 / 商业化）+ 长篇形态特化；读者团按口味多实例并行，**跨口味分歧不合并、不取平均**
- **角色层收敛为三角色原型**：`ai-team-role-planner` / `-maker` / `-reviewer` 作领域无关基座，领域扩展覆盖差异；原型归属由 `role-registry.md` 声明，不靠名字推断
- **废除「轻量 / 标准 / 完整」复杂度档**：改为**一次弹窗的用户多选编排**（按原型分组，1 角色 / 维度 1 项）+ 领域预勾选推荐
- **审查者按维度多实例**：每维度独立检查清单与报告，可声明前置依赖（如代码审查依赖测试验证）；并行且不合并
- **领域标准资产化**：判据集中存放 `skills/ai-team-write/standards/`（7 份）作**单一事实源**，角色按需 `read_file`，skill 内不复制数值 / 阈值 / 枚举
- **统一参数契约**：全部角色由编排方注入 `领域` / `变体` / `artifact_dir` / `upstream` 等，**角色内不再出现领域硬编码路径**

### 09-18 · 命名规范与编排细节收敛

- **命名规范**：`ai-team-{领域}-{类型}-{目标}-{对象}`——通用角色 / 工具不带领域段；`ai-team-pt-hm-*` → `ai-team-dev-pt-hm-*`（12 个）；平台特化的**角色**补齐 `role-`（3 个），与领域角色名逐字配对
- **编排弹窗粒度**：write 由「按原型合并 3 项」改为「逐角色 / 维度列项 7 项」，并为读者口味补一问
- **接通流程复盘**：`ai-team-tool-auto-tune` 改为 PM 收尾按需触发（用户弹窗决定），只收集**流程层**反馈、产 `optimization-report.md`，**不改 skill、不写 CHANGELOG**
- **引入框架版本号**：2.0 / 1.0，标注于根 `README.md` 与三个编排入口；`CHANGELOG.md` 改按版本分节

### 09-18 · 流程审计修复

- **补齐 PM 对「阻塞」信号的处置**（根因）：原门禁只按置信度分行、未按信号类型分支 → `global-rule` §七 新增处置（用户三选一：补齐上游 / 跳过 / 终止）
- **解除 3 处死路**：未勾计划者时 reviewer 必阻塞、需求文本被当文件路径读、断点台账 `progress.md` 有读无写（含「回收后才写」）
- **补齐无源参数与无效注入**：write 补 `题材` / `平台` / `范围` 来源；planner 去掉无人读的 `form`；maker / reviewer 补 `阶段`
- **两领域对齐**：dev 补「领域门禁」段与回炉 **3 轮**上限；`tool-report` 补写作报告模板与产出物清单
- **文档对齐 2.0**：4 份 README 清除 1.0 遗留（复杂度档 / 四选一 / Q4 等）

## 1.0 · ～2026-09-17

### 09-17 · 调试定位手段分级 + 用户协作成本优先

- **`ai-team-tool-debug-loop`**：新增「步骤 0：定位手段选择（成本递增）」——代码路径推理 → 已有日志 → 自动化复现 → 最小化诊断日志 → 二分/回滚；门禁：不得跳过前 3 项直接埋点，也不得停在代码推理就断言根因；平台日志读取改为实测可用链路
- **`ai-team-tool-global-rule`**：新增「用户协作成本优先」——用户 ≤1 步能完成、而 AI 需 ≥3 步或依赖脆弱定位时，优先给选项请用户操作并说明原因

### 09-15 · 测试门禁补齐

- **边界维度清单**：用例设计改为按 8 维度逐项给结论（空与缺失 / 数值与分页 / 阈值临界 / 状态×触发矩阵 / 幂等 / 时序竞态 / 异常路径 / 数据可构造性），只写「覆盖边界」不算执行
- **结论有效性**：构建产物须晚于源码改动、安装后须确保运行新版本、代码再变更则受影响结论必须重跑或标注失效
- **依赖用户操作的环境条件**：设计期枚举用户操作清单 → 合并为一次询问 → 执行前校验条件已生效 → 执行后恢复并核对
- **通用内核**：「是否含需求人员」并入规模选项，一次提问给出「单角色/标准 × 含/不含」四选一（2.0 已废除，见上）

### 09-11 · 通用内核编排升级

- **一次 ask 多问同出**：角色配置 + 流程运行模式合并为一次提问，不再多轮打断用户
- **预估角色推导**：弹窗前基于用户原始问题推导候选清单，仅用于填充选项文字（不落盘、不作为权威拆解）
- **团队调整机制**：designer 深挖需求后可向用户建议增删角色，最终清单覆盖初始选择
- **designer 分层**：拆为通用基座 + 开发领域扩展 `ai-team-dev-role-designer`
- **PM 纳入全局约束**：PM 也须显式加载 `ai-team-tool-global-rule`，其弹窗同受选项规范约束
- **选项规范统一**：末位「自定义填写」由全局规范统一追加，各 skill 不再自行列出
- **新增「合理质疑与风险提示」**：发现明显问题时先提示（问题 + 理由 + 替代方案），用户确认后完整执行、不反复劝阻
- **`ai-team-tool-report` 参数化**：写入路径改用 PM 传入的 `artifact_dir`，消除通用场景的 dev 路径耦合

### 09-09 · designer 改为按需确认

- 进入动态编排后先询问用户是否需要需求分析；选「不需要」则跳过 designer，直接以原始问题进入角色推导

### 08-20 · 流程运行模式 + PM 职责再收窄

- **新增全自动 / 手动确认**：手动确认下角色完成先向用户确认完成度，确认后才通知 PM 放行下一角色
- **PM 只做调度**：任务拆解下沉 designer；PM 不读角色报告全文，仅依据完成信号（置信度 / 产出路径 / 摘要）调度

### 08-19 · ArkTS 编码规则补充

- 澄清 `@ObservedV2` / `@Trace` 的观测能力与字段类型限制
- 新增「字符串拼接优先使用模板字符串」规范（规避 `+` 的类型歧义）

### 08-18 · 通用内核诞生 + 角色组合器

- **新增 `ai-team` 通用编排内核**：`domain-map.md` 领域路由 + 动态角色编排，开发领域作为插件挂载
- **新增 `ai-team-tool-role-composer`**：从子目标所需视角反推角色组合，含四要素规范、数量门禁与 5 类反模式
- **体系换位重命名**：`ai-team-core` → `ai-team`（内核坐总入口），原 `ai-team` → `ai-team-dev`（开发领域插件）
- **借鉴 addyosmani/agent-skills**：description 双段式改造、编排深度门禁（最多 1 层）、上下文信任分级与注入防护、reviewer 对抗式审查 + RECONCILE 四分类、偷懒借口反驳表

### 08-13 · 审查增强

- reviewer 问题分级：🔴 blocker / 🟡 suggestion / 💭 nit，反馈遵循「为什么 + 建议」格式
- 新增「可靠性 & 可观测性审查」：错误处理覆盖、降级路径、重试/幂等、结构化日志

### 08-10 · 任务拆分 + 阶段式开发

- 复杂需求由 designer 判定并产出「任务清单」表格、经用户确认
- coder 逐个串行开发（每任务编译验证 + 向用户汇报确认），全部完成后汇总 `coder-report.md`
- PM 编排入口零改动；无任务清单时完全走单任务原流程

### 08-07 · 极简编码规范

- 新增 `ai-team-tool-minimal-code`：懒惰阶梯 7 级、根因修复门禁、不精简清单、`minimal:` 债务注释约定、过度设计审查五标签（`delete`/`stdlib`/`native`/`yagni`/`shrink`）
- coder 编码规则与 reviewer 质量审查接入
